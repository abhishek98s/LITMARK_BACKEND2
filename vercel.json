{
    "version": 2,
    "builds": [
        {
            "src": "src/index.ts",
            "use": "@vercel/node",
            "config": {
                "distDir": ".vercel/output",
                "buildCommand": "npm run build",
                "installCommand": "npm ci",
                "devCommand": "npm run start"
            }
        }
    ]
}